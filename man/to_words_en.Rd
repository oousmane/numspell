% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_words_en.R
\name{to_words_en}
\alias{to_words_en}
\title{English number words}
\usage{
to_words_en(
  x,
  style = c("us", "uk"),
  decimal = c("digits", "cents"),
  unit = NULL,
  unit_plural = NULL,
  subunit = "cent"
)
}
\arguments{
\item{x}{Numeric vector.}

\item{style}{Either \code{"us"} (default, no mandatory "and") or \code{"uk"}
(inserts "and" for numbers like 101 -> "one hundred and one").}

\item{decimal}{One of \code{"digits"} or \code{"cents"} (currency-like; two decimals).}

\item{unit}{Optional major unit for integers in \code{"digits"} mode (e.g., "day"/"days").}

\item{unit_plural}{Optional explicit plural for \code{unit}.}

\item{subunit}{Subunit label for \code{"cents"} mode (default \code{"cent"}).}
}
\value{
Character vector.
}
\description{
Convert numbers to English words with hyphenation (e.g., "twenty-one"),
short scale (thousand, million, billion), negatives, and decimal handling.
}
\examples{
to_words_en(21)                         # "twenty-one"
to_words_en(101, style="uk")            # "one hundred and one"
to_words_en(8, unit="days")             # "eight days"
to_words_en(1523.5, decimal="cents", unit="dollar", subunit="cent")
}
