% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/to_words_fr.R
\name{to_words_fr}
\alias{to_words_fr}
\title{French number words}
\usage{
to_words_fr(
  x,
  decimal = c("digits", "centimes"),
  unit = NULL,
  unit_plural = NULL,
  subunit = "centime"
)
}
\arguments{
\item{x}{Numeric vector.}

\item{decimal}{One of \code{"digits"} (read each decimal digit) or \code{"centimes"}
(currency-like formatting using two decimals).}

\item{unit}{Optional major unit to append for integers in \code{"digits"} mode
(e.g., \code{"jour"} or \code{"jours"}). If supplied, pluralization is handled
heuristically or via \code{unit_plural}.}

\item{unit_plural}{Optional explicit plural for \code{unit} (e.g., \code{"chevaux"}).}

\item{subunit}{Subunit label for \code{"centimes"} mode (default \code{"centime"}).}
}
\value{
Character vector.
}
\description{
Convert numbers to French words with correct hyphenation and pluralization
(e.g., \code{cent(s)}, \code{quatre-vingt(s)}, \verb{soixante et onze}). Supports
negatives, up to |n| < 1e12, and decimal handling.
}
\examples{
to_words_fr(8, unit = "jours")     # "huit jours"
to_words_fr(81)                    # "quatre-vingt-un"
to_words_fr(80)                    # "quatre-vingts"
to_words_fr(200)                   # "deux cents"
to_words_fr(201)                   # "deux cent un"
to_words_fr(12.305, decimal="digits")
to_words_fr(1523.5, decimal="centimes", unit="euro", subunit="centime")
}
